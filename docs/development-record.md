# 开发记录

日期：2026-08-24

## 开发约定

- 后续开发过程中新增的注释和文档统一使用中文。
- 技术名词、类名、方法名、命令、插件 ID 和外部链接可以保留英文原文。

## 已实现内容

- 创建 IntelliJ Platform 插件项目骨架。
- 增加 Gradle Kotlin DSL 配置，目标 IDE 为 IntelliJ IDEA `2026.1.3`。
- 设置 Java/Kotlin toolchain 为 Java 21。
- 设置插件兼容分支为 IntelliJ Platform `261`。
- 在 `META-INF/plugin.xml` 中增加插件元信息和 IntelliJ 注册项。
- 增加 `Tools -> Novel Reader` 菜单入口。
- 增加右侧 `Novel Reader` 工具窗口。
- 增加 TXT 读取器，支持 UTF-8、GB18030、GBK 编码回退。
- 增加 EPUB 读取器，支持按 OPF spine 顺序提取 XHTML 正文。
- 增加章节解析器，支持常见中文章节标题和 `Chapter N`。
- 增加 Swing 阅读面板，包含：
  - 打开 TXT 或 EPUB
  - 上一章
  - 章节选择
  - 下一章
  - 字号减小 / 增大
  - 行距减小 / 增大
  - 字体选择
  - 文字颜色选择
  - 主题选择
  - 阅读宽度选择
  - 隐藏光标开关
  - 状态显示
- 增加项目级阅读状态持久化：
  - 最后打开文件路径
  - 编码
  - 当前章节索引
  - 滚动位置
  - 字体
  - 文字颜色
  - 字号
  - 行距
  - 主题
  - 阅读宽度
  - 隐藏光标开关
  - 按钮显示方式
- 增加 `.gitignore` 和项目 `README.md`。

## 新增文件

- `.gitignore`
- `README.md`
- `settings.gradle.kts`
- `build.gradle.kts`
- `gradle.properties`
- `docs/requirements-and-design.md`
- `docs/development-record.md`
- `src/main/resources/META-INF/plugin.xml`
- `src/main/resources/icons/reader.svg`
- `src/main/kotlin/com/chen/reader/ChapterParser.kt`
- `src/main/kotlin/com/chen/reader/BookLoader.kt`
- `src/main/kotlin/com/chen/reader/EpubBookLoader.kt`
- `src/main/kotlin/com/chen/reader/NeatReaderPanel.kt`
- `src/main/kotlin/com/chen/reader/NovelReaderOpener.kt`
- `src/main/kotlin/com/chen/reader/NovelReaderToolWindowFactory.kt`
- `src/main/kotlin/com/chen/reader/OpenNeatReaderAction.kt`
- `src/main/kotlin/com/chen/reader/OpenNovelAction.kt`
- `src/main/kotlin/com/chen/reader/ReaderPanel.kt`
- `src/main/kotlin/com/chen/reader/ReaderStateService.kt`
- `src/main/kotlin/com/chen/reader/TxtBookLoader.kt`
- `src/main/kotlin/com/chen/reader/model/Book.kt`

## 已执行验证

- 编辑前检查仓库状态：仓库除 `.git` 外为空。
- 使用 `rg --files` 检查生成文件清单。
- 使用 `rg` 检查是否存在遗留标记或旧配置。
- 检查 `plugin.xml` 中是否包含预期的 Action 和 Tool Window 注册。
- 检查本机构建工具可用性：
  - `gradle --version` 失败，原因是 `gradle` 未安装或不在 `PATH` 中。
  - `kotlinc -version` 失败，原因是 `kotlinc` 未安装或不在 `PATH` 中。
  - `GRADLE_HOME` 未设置。
  - 初始检查时，当前用户 Gradle 缓存中没有可复用的 IntelliJ Platform Gradle Plugin 缓存。
- 使用 IntelliJ IDEA 2026.1.3 自带 JBR 运行 `buildPlugin`，构建成功。

## 初始未完成验证

- 初始阶段未能运行 Gradle 编译。
- 初始阶段未能运行 `buildPlugin`。
- 初始阶段未生成插件 ZIP。
- 初始阶段未在 IntelliJ IDEA 2026.1.3 中手动安装验证。

## 修复和迭代记录

- 2026-08-24：修复 Gradle settings 配置。项目导入时报错 `settings.gradle.kts` 中 `intellijPlatform` 和 `defaultRepositories` 无法解析，已增加官方 `org.jetbrains.intellij.platform.settings` 插件，并将 IntelliJ Platform 插件版本声明移动到 settings 中。
- 2026-08-24：移除过期的 IntelliJ Platform 依赖辅助函数。Gradle 报错无法解析 `instrumentationTools()`，IntelliJ Platform Gradle Plugin 2.18.1 不再需要该函数处理代码插桩。
- 2026-08-24：修复 Kotlin 编译失败。目标 IntelliJ Platform 中没有 `UIUtil.getTextAreaBackground()`，改为使用 Swing `UIManager.getColor("TextArea.background")`。
- 2026-08-24：使用 IDEA 2026.1.3 自带 JBR 作为 Gradle 运行时验证 `buildPlugin`，构建成功，并在 `build/distributions/` 下生成插件 ZIP。
- 2026-08-24：成功启动 `runIde`。沙箱 IntelliJ IDEA 2026.1.3 Welcome 窗口已打开，打开项目时到达信任项目弹窗，并在信任前取消 Microsoft Defender 排除列表勾选。后续 UI 验证因用户按下 Escape 停止 Computer Use 而中止。
- 2026-08-24：修复 `Novel Reader` 工具窗口变窄时顶部工具栏被裁剪的问题。操作按钮保留在第一行，章节选择器独占第二行，避免章节选择器和后续按钮被隐藏。
- 2026-08-24：增加阅读舒适度控制。阅读区改用 `JTextPane` 和段落样式，保留字号控制，增加行距控制，持久化行距，并在状态栏显示当前字号和行距。
- 2026-08-24：增加章节边界连续滚动和字体选择。章节末尾继续向下滚动时进入下一章，章节开头继续向上滚动时进入上一章末尾。阅读器列出已安装系统字体，并优先展示常见中文字体，选择后立即应用并持久化。
- 2026-08-24：增加阅读主题、阅读宽度、进度显示、阅读快捷键和隐藏光标开关。状态栏显示本章进度和全书进度；阅读区支持主题色、最大阅读宽度模拟、翻页/章节/字号/行距快捷键；隐藏光标开关会在阅读区内使用透明鼠标光标，并保持文本选区可见。
- 2026-08-24：压缩阅读窗口工具栏高度。默认只显示打开、上一章、章节选择、下一章和设置按钮；字号、行距、字体、主题、宽度和隐藏光标等高级设置移动到可展开设置区，默认隐藏。
- 2026-08-24：精简底部状态栏，移除字体、字号、行距等设置类信息，仅保留文件名、章节、本章进度、全书进度和编码。
- 2026-08-24：增加正文文字颜色选择和按钮显示方式切换。设置区新增文字颜色下拉框；工具栏按钮支持文字模式和简略图标模式；`Tools -> Novel Reader` 子菜单新增按钮格式切换入口；图标按钮均设置鼠标悬浮提示。
- 2026-08-24：优化章节识别规则，避免正文中类似“第四回中...”的叙述句被误识别为章节标题。章节候选行现在会过滤过长标题、明显句中标点，以及章回标记后紧跟叙述连接词的行。
- 2026-08-24：压缩章节下拉框宽度。章节选择器不再占满工具栏横向空间，默认只显示 `第57回`、`Chapter 1` 这类短章节标识；完整章节标题保留在鼠标悬浮提示中。
- 2026-08-24：修复隐藏光标模式下的键盘阅读体验。阅读正文改用不可见文本光标，避免点击正文后竖线光标再次出现；方向键绑定到阅读面板，勾选隐藏光标或调整设置后会自动把焦点交还阅读区，上下键可直接滚动并在章节边界连续切章。
- 2026-08-24：修复正文获取焦点后方向键仍优先移动隐藏文本光标的问题。阅读快捷键现在同时覆盖正文组件的聚焦输入映射，方向键会直接滚动阅读区，不再等待不可见光标移动到可视边界。
- 2026-08-25：创建 `codex/dictionary-lookup` 功能分支，并将插件版本号提升到 `0.2.0`。
- 2026-08-25：增加阅读区划词右键菜单。选中文字后可直接进行汉典查词、DeepL 翻译、浏览器搜索和复制；没有选中文本时相关菜单项自动禁用。外部查找通过 IntelliJ Platform 浏览器工具打开，避免依赖未公开翻译接口。
- 2026-08-25：根据新的查词需求调整划词菜单。去掉 DeepL 翻译入口，将浏览器搜索改为百度搜索；加入 `chinese-xinhua` 的 `word.json` 和 `ci.json` 作为本地词典资源，并保留 MIT License 文件。
- 2026-08-25：将插件版本号提升到 `0.2.1`。本地词典查找采用后台任务和首次懒加载，查到时弹窗显示词条，查不到时弹窗提示未找到。
- 2026-08-25：将插件版本号提升到 `0.2.2`。阅读区从单章替换渲染改为整本书连续渲染，章节选择、上一章和下一章改为滚动定位；滚动时按可视文本偏移自动更新当前章节和进度，解决章节切换生硬、无法同时查看章节尾部与下一章开头的问题。
- 2026-08-25：将插件版本号提升到 `0.2.3`。阅读记录从滚动条像素值升级为全书字符偏移、原文锚点和章节内千分比组合；恢复时优先按锚点校验定位，锚点失效时按章节进度兜底，旧 `scrollValue` 保留为兼容字段。
- 2026-08-25：将插件版本号提升到 `0.2.4`。阅读区从整本书连续渲染改为章节窗口化渲染，每次只渲染当前章节全文和上下文预览；章节状态、阅读进度和阅读记录统一按视口 25% 高度处的阅读锚点计算，窗口大小或阅读样式变化后延迟按锚点重新定位，减少重排卡顿和定位漂移。
- 2026-08-25：将插件版本号提升到 `0.2.5`。阅读正文右侧增加额外选择缓冲边距，避免行尾最后一个字靠近滚动条或边框时没有足够鼠标落点，导致无法选中后查词。
- 2026-08-27：将插件版本号提升到 `0.2.6`。鼠标滚轮或键盘滚动接近章节渲染窗口顶部/底部时，提前切换到相邻章节窗口，并在新窗口布局完成后补偿一次同方向滚动，减少章节切换期间旧窗口边界导致的滚轮空转。
- 2026-08-28：将插件版本号提升到 `0.2.7`。针对章节衔接处滚轮抽搐问题，取消接近边界时的提前切换，改为滚动条真实到达渲染窗口顶部或底部后再切换相邻窗口；鼠标滚轮状态更新延迟到 Swing 默认滚动之后执行，避免滚轮事件、滚动条回写和窗口切换互相抢状态。
- 2026-08-28：将插件版本号提升到 `0.2.8`。修正章节窗口和当前章节状态互相覆盖的问题：滚动进入上下文预览区时只更新章节下拉框、进度和阅读锚点，不再立即重建渲染窗口；只有滚动条真实到达当前渲染窗口边缘时，才按视口边缘文本所在章节切换窗口，避免继续下滑时被旧窗口章节拉回上一章。
- 2026-09-04：将插件版本号提升到 `0.3.0-alpha.1`。正文阅读区从 `JTextPane` 章节窗口替换架构改为全书虚拟阅读组件：滚动条对应整本书布局高度，章节仅作为导航和进度标记；组件只绘制当前可见行，并自行处理拖选、高亮、复制和右键查词，避免章节边界重设大段文本和重新段落布局导致卡顿。
- 2026-09-04：将插件版本号提升到 `0.3.0-alpha.2`。修复窗口缩放或阅读样式变化时章节漂移的问题：重排前锁定当前可视阅读锚点，重排期间暂停滚动条回写，重排完成后再按全书 offset 恢复视口；同时新增正文加粗开关并持久化。
- 2026-09-04：将插件版本号提升到 `0.3.0-beta.1`。完善虚拟阅读组件：窗口连续缩放时只更新边距并延迟整本重排，缩放结束后统一重排并恢复锚点；每行缓存字符横向位置，拖选和选区绘制不再反复计算子串宽度，提升行尾选字、跨行拖选和右键查词稳定性。
- 2026-09-04：将插件版本号提升到 `0.3.0`。正式收口虚拟阅读器版本：加粗控件移动到字号和行距调整之前；按钮格式切换为图标模式时，加粗使用字母 `B` 图标显示，文字按钮模式下显示中文“加粗”。
- 2026-09-04：将插件版本号提升到 `0.3.1`。将单一加粗开关升级为多档字重下拉框，提供“标准、半粗、加粗、特粗”四种规格；图标按钮模式下使用 `标准、B-、B、B+` 显示，文字按钮模式下显示中文规格名称。
- 2026-09-14：将插件版本号提升到 `0.3.2`。修复重启电脑或重新打开同一本 TXT 后阅读记忆失效的问题：打开文件时先判断是否为上次阅读的同一本书，同书打开不再清空章节、全书 offset 和锚点；恢复滚动位置时如果工具窗口尚未完成布局，则延迟到滚动范围可用后再定位，避免初始化阶段把阅读位置误写回开头。
- 2026-09-16：将插件版本号提升到 `0.4.0`。增加 EPUB 格式支持：通过 `BookLoader` 统一分发 TXT/EPUB，EPUB 读取器解析 `container.xml`、OPF manifest 和 spine，按阅读顺序提取 XHTML 正文并生成章节；现有虚拟阅读、章节导航、进度、划词查词和阅读记忆能力继续复用。
- 2026-09-16：将插件版本号提升到 `0.4.1`。增强 EPUB 脚注和复杂排版的纯文本还原：正文脚注链接显示为 `[注N]`，章节末尾追加“【注释】”；标题、列表、引用、表格、图片 alt、ruby 注音、粗体和斜体会转换为更可读的纯文本结构。
- 2026-09-17：将插件版本号提升到 `0.4.2`。工具窗口新增“Neat Reader”Tab，使用 IntelliJ JCEF 内嵌 `https://www.neat-reader.cn/webapp`；新增 `Tools -> Novel Reader -> 打开 Neat Reader` 菜单入口；JCEF 不可用时在 Tab 内提示并提供外部浏览器打开入口。
- 2026-09-17：将插件版本号提升到 `0.4.3`。优化 Neat Reader 内嵌网页体验：按工具窗口宽度自动设置 JCEF 缩放，增加缩小、自适应和放大按钮；拦截 JCEF 新窗口请求、`window.open` 和 `target="_blank"` 链接，优先在当前 Tab 打开，避免云端下载或打开书籍后出现空白弹窗。
- 2026-09-18：将插件版本号提升到 `0.4.4`。尝试修复 Neat Reader 网页在窄工具窗口内显示不完整、缩放按钮数值变化但网页版式未实际收缩的问题：改为在页面内注入 viewport、横向溢出控制和宽度测量脚本，根据工具窗口宽度对网页正文做页面级自适应。后续验证发现该方案会把 Neat Reader 阅读器正文整体压得过小，不适合作为默认方案。
- 2026-09-18：将插件版本号提升到 `0.4.5`。回退 Neat Reader 的激进页面级适配脚本，恢复 JCEF 浏览器级缩放；同时把默认缩放调回 100%，窄窗口下仅做温和缩小，优先保证打开书籍后的正文可读性。
- 2026-09-18：将插件版本号提升到 `0.4.6`。修复 Neat Reader 工具窗口稍微放大后自动缩放升到 100%，触发网站自身宽屏阅读版式导致正文变窄的问题；自动模式最高保持约 96%，手动放大仍保留。
- 2026-09-20：将插件版本号提升到 `0.4.7`。重做 Neat Reader 内嵌网页适配：① 缩放不再按窗口宽度直接映射，改为先算「站点看到的 CSS 视口宽度」（`cssWidth = 窗口像素宽 / zoomFactor`），把它约束在 `[600, 1024]` 区间内再反推缩放倍率，量化到 0.4 zoomLevel 的档位网格（相邻档约 7.6%）并加入 40 CSS px 迟滞带，解决档位过密「调了没反应」和临界抖动；② 按用户确认的「保不错版」目标修正方向——窗口宽于上界时是**放大**渲染压住 CSS 像素，而不是像 0.4.6 那样缩小到 96%（缩小渲染会让站点以为窗口更宽，反而更容易触发宽屏版式）；③ 窗口 resize 改为 150 ms 防抖，拖动期间只重启定时器不动缩放；④ 新窗口拦截脚本删除构造时盲注，改为 `onLoadEnd` 事件驱动唯一注入点，跳过 `about:blank`、按 URL 变化重新注入，并用探针校验、未生效最多补 2 次；⑤ 手动缩放持久化到 `ReaderState` 并锁定，「自适应」按钮改名为「跟随窗口」，工具栏显示「已锁定」；⑥ JCEF 浏览器延后到组件挂载（`addNotify`）时才创建，构造阶段只放占位组件；⑦ JCEF 不可用时补充「重试」和「外部打开」两个兜底按钮；⑧ 新增缩放诊断日志与站点实测宽度回报，用于真机测量站点断点、回填档位表常量。

- 2026-09-21：将插件版本号提升到 `0.5.0`（**`Book` 数据结构变化，按项目惯例属于 breaking change**）。分三批完成 EPUB 图片与注解能力：
  - **第一批（数据层 T30–T33）**：`Book` 从「一整段纯文本」改为结构化 `Block` 流（`TextBlock` / `ImageBlock` / `InlineImageBlock` / `CaptionBlock` / `FootnoteRefBlock` / `FootnoteBodyBlock`），`plainText` 由 blocks 拼接派生，`content` 保留为过渡别名，因此约 10 处 `content` 调用点与阅读位置恢复（全书 offset / 原文锚点 / 千分比）**语义零变化**。新增 `book/BookResources.kt`：图片内建宽高只读文件头探测（**不解码**，满足设计文档 §8），位图按像素预算做 LRU 缓存且只在后台线程解码。EPUB manifest 开始收集图片资源，不再整类丢弃。
  - **脚注识别（T32b/T32c）**：新增「双向互指」通道——两个锚点满足 `A.href#frag == B.id && B.href#frag == A.id` 才算成对，**不依赖 `s`/`d` 命名前缀**，再要求两端锚文本解析出同一个 `[N]`。配套 8 条排除规则（目录页整页跳过、`<nav>` 内逐条排除、长文本交叉引用、返回回链、外链、图片链接、悬空链接、自指）与两通道元素级去重；注释编号改用书里原本的标记而不是位置序号，并把原标记存进 `FootnoteRefBlock.label` / `FootnoteHotSpot.label`。通道 A 从正则改为 DOM 遍历 + 同名标签配对计数，修掉嵌套同名标签把注释正文截断一半的问题。
  - **第二批（渲染层 T40–T43）**：`VirtualReaderPane` 与 `VirtualLine` 搬到 `ui/virtual/`（`VirtualLine` 演进为 `LayoutElement` 密封接口）。排版从「所有行等高、`y = 行序号 × 行高`」改为**按 Block 流排版 + y 游标累积**；`lineIndexForY` 改二分查找，`getPreferredSize` 用累积 `contentHeight`，`paintComponent` 按元素类型分派。图片高度只用 `ResourceMeta` 换算，**解码在后台线程，回调只 `repaint()`，绝不 relayout**。
  - **第三批（交互层 T45/T64/T66/T67）**：新增 `hotSpotAt(point)`，鼠标优先判热区（命中则不进选区逻辑）；热区手型光标**优先于隐藏光标**，否则开了隐藏光标就点不到注解。新增 `ui/FootnotePopup.kt`（点击脚注引用弹窗显示注释正文，数据自足、不做跳转，标题保留书里原标记）和 `ui/ImageLightbox.kt`（图片灯箱，首屏复用已解码小图、后台再解高清，支持滚轮缩放、拖拽平移、1:1、适配、Esc）。弹层关闭后归还焦点，且弹层期间忽略 viewport resize，避免走 0.3.2 那条「重排 → 恢复 → 写回」回路。
- 2026-09-21：将插件版本号提升到 `0.5.1`。修复灯箱与正文图片被放大模糊：`rasterize` 拆成 `rasterizeScaled`（`targetWidth > 0`，走像素预算 LRU）与 `rasterizeFull`（原图档位，走新增的软引用缓存），两档互不串。根因是原缓存 key 只有 `resourceId` 且命中即无条件返回——灯箱请求原图时拿到的是阅读区按显示宽度解码的小图，放大后必然模糊；同一个 bug 还导致窗口拉宽后正文一直显示早先按窄宽度解码的糊图、永不重解（现改为缓存位图窄于请求宽度时重新解码，`effectiveWidth` 用 `min(targetWidth, entry.width)` 夹住）。灯箱改为首屏即 `fitToPanel()`，新增 `autoFit` + `componentResized` 重 fit，用户手动缩放后 `autoFit = false`，高清图到达不再冲掉其视图。另加诊断日志，搜 `图片灯箱解码完成` 可看到位图实际宽高、面板尺寸与缩放比。
- 2026-09-21：将插件版本号提升到 `0.5.2`。让脚注引用在正文里**颜色与正文区分**，不再混作一堆同色文字：
  - 绘制从「整行一次性单色 `drawString`」改为「默认整行一次绘制，**只有这一行里确实含脚注时**才切分成普通片段 / 脚注片段依次绘制」。默认路径与改造前完全一致——这本书正文有五千多行、绝大多数行没有脚注，不能为几个标记让每行都变慢。
  - 着色对象是两类：正文 `FootnoteRefBlock` 的 `[注N]` 整块，以及章末 `FootnoteBodyBlock` 开头的 `[注N]` 前缀（前缀长度直接复用 `FootnoteBodyBlock.markerLength`，与 `plainContentOf` 同一份定义，避免 UI 层另写一遍字符串拼接而串味）。除颜色外还加了一条贴基线的下划线，视觉语言更接近「这是可点的东西」。
  - 强调色**随主题换算**而不是写死一个色值：浅底取深靛蓝 `#0A5AA8`、深底取亮天蓝 `#6FB8F5`，再按 WCAG 相对亮度做对比度兜底，不足 3:1 就朝远离背景的方向逐步加深 / 提亮。三个内置主题的对比度都在 6:1 以上，兜底**一次都不会触发**；真正被它救回来的是中灰色底（感知亮度 0.502 会被判成浅底拿到深靛蓝，实际对比度却只有 1.75:1，几近不可见）。
  - `paintSelection` 仍在文本之前绘制，这段只改 `g.color` 与绘制子串，并在结束时复位颜色；位置恢复相关代码（`offsetAtY` / `scrollValueForOffset` / `offsetAtPoint` / `viewportAnchorOffset` / `restoreOffset`）**一行未动**。唯一的接口变化是 `updateReaderStyle` 新增 `background` 参数——主题切换要立刻重算强调色。
  - 实测（临时探针，验完已删，覆盖三本真书含一本 Pandoc 系 0 引用的书）：标记区间在 `plainText` 中逐个字符校验，156/156 精确落在 `[注N]` 上；行级裁剪的二分版与朴素线性扫描版在真实行、24 字符细粒度行、2 万随机线段和每个标记的边界线段上**逐段等价**；把一行按片段拼回去能无损还原整行原文。
- 2026-09-21：将插件版本号提升到 `0.6.0`（**`Book.plainText` 内容变化，按项目惯例属于 breaking change**）。修复脚注引用在正文里被**强制换行**、且前后多出空行（用户截图反馈）：
  - **根因**：`EpubBookLoader.stripStructuredMarkup` 把 XHTML **源码的缩进换行**当成硬换行保留了下来。EPUB 的 XHTML 普遍是缩进排版的，段落里行内元素（最典型就是脚注引用 `<a>`）前后各有一个源码换行，于是正文被切成「前文 / `[注3]` / 后文」三块；渲染层 0.5.0 起「一个块一律另起一行」，`[注3]` 就被顶到单独一行，段落还被拆出多余空行。
  - **修复 1（数据层）**：`stripStructuredMarkup` 的**最开头**新增 `foldSourceNewlines`，把 `\r?\n` 连同两侧水平空白折叠成单个空格。必须排在所有 replace 之前——`<br>`（→ `\n`）和块级标签（→ `\n`）产生的才是**语义换行**，要排在折叠之后才不会被吃掉。`<pre>` 例外：那里的换行是内容而非排版，按区间保护原样保留。
  - **修复 2（渲染层）**：`VirtualReaderPane.layoutBlocks` 把连续的**可内联块**（`TextBlock` / `CaptionBlock` / `FootnoteRefBlock` / `FootnoteBodyBlock`）先并成一段再折行，段内文本取 `plainText.substring(首块.plainStart, 末块.plainEnd)`（块在 plainText 中连续不重叠，所以这就是该段的精确内容）。图片块仍然独立成区。段落分隔**不依赖块边界**、只靠块内容里自带的 `\n`，因此合并不会把段落粘连，只是去掉了「块边界」这个伪换行。
  - **顺带修掉一个命中测试精度问题**：`hotSpotInLine` 的收尾条件从 `spot.plainStart > line.endOffset` 收紧为 `>=`。原先热区与本行**字符交集为空**时（典型是图片占位符紧邻的空行）像素区间退化成 `[W, W]`，点击那条空行也会弹灯箱——叔本华那本书 121 张图就带出 93 处这种死区。字符交集非空 ⇒ 像素区间也非空（`xPositions` 每步至少 +1 px），所以收紧不会丢掉任何真实命中。
  - 实测（临时探针，验完已删，三本真书：`b1` 叔本华 78 引用 / `b2` AI-Agents 0 引用 / `b3` [德]叔本华 121 行内图）：①「被换行包围的脚注引用」`0 / 78`（修复前 78/78）；②`吠檀多哲学 [注3] 被认为是毗耶舍的作品` 现已排在同一行；③排版五条不变量（y 升序不重叠 / offset 升序 / 覆盖全文 / 总高一致 / offset↔y 往返与单调）三本书全为 true；④脚注命中 `78 / 78`，无标记行采样约 260 万点，**真实误报 0**（剩余 11 处是 `[注11]` 这类标记被软换行拆开后位于行首/行尾的合法命中，见下面第 5 点）；⑤段落分隔未丢失：三本书 `plainText` 中的 `\n\n` 分别为 1431 / 3299 / 2302，渲染出的空行 1446 / 3299 / 2395，均 ≥ 段落分隔数。

- 2026-09-23：将插件版本号提升到 `0.7.0`（**`Book.plainText` 内容变化，按项目惯例属于 breaking change**：b1 的正文因乱码还原而变短）。修复 EPUB 章节标题乱码（`ä½œä¸º…` 这种串，用户截图反馈）：
  - **根因不是"判错编码"，而是"书本身被二次转码过"**（与最初判断不同，实测后修正）。b1 这本书的 `OEBPS/text00000.html` **完全没有 encoding 声明**，字节也**确实是合法 UTF-8**——按 UTF-8 解出来的结果忠实等于文件里的内容，错的不是解码而是**文件内容**：原始 UTF-8 字节先被上游当成某个单字节字符集读了一遍，得到的乱码字符又被按 UTF-8 存进了文件（`<title>` 原始字节 `C3 A4 C2 BD C5 93…`，即「ä½œ」本身被 UTF-8 编码；其中 `C5 93`(œ) 是 cp1252 的 0x9C，ISO-8859-1 里没有，由此反推误读字符集是 Windows-1252）。所以只改 `detectCharset` 救不了它。
  - **改动 1：`detectCharset` 改为强信号优先**（仍有独立价值——它修的是"声明与实际不一致"这类真·编码误判）。顺序：BOM → **UTF-8 严格解码是否通过**（多字节序列恰好全合法是极强信号；GB18030 几乎能"解出"任意字节流，所以"声明的 GB 能解通"是弱信号）→ 声明编码（走**白名单**映射，不用 `Charset.forName` 吃任意字符串；GB 三兄弟统一映射 GB18030 超集避免生僻字丢码）→ GB18030 → Big5 → UTF-8 兜底。"严格"指 `CodingErrorAction.REPORT`，静默替换成 U+FFFD 会让弱信号永远为真。
  - **改动 2：`parseXhtml` 改走 `InputSource(StringReader)` 字符流**。原实现把已解码正确的 `markup` 再 `toByteArray(utf8)` 喂给 DOM，解析器会照着里面那句陈旧的 `<?xml encoding="gb2312"?>` **二次解码**，刚修好的内容又被打回 mojibake，且这条路径绕过 `detectCharset`，改编码探测救不了。
  - **改动 3（真正修好 b1 的那一刀）：新增 `repairMojibake`**，在 `decodeMarkup` 之后把乱码字符按反向映射还原。三个门槛缺一不动：① 整段只由 cp1252 高位字符组成；② 按反向映射编回字节后能被 UTF-8 **严格**解码；③ 还原结果里出现中日韩字符（最能排除误伤——正常法语/西语重音词变不出汉字）。**反向映射必须自己写，不能用 `Charset.forName("windows-1252").newEncoder()`**：Java 的严格实现把 0x81/0x8D/0x8F/0x90/0x9D 五个未定义槽位判为不可映射，而现实转码工具通常按 Latin-1 恒等映射；「意」的 UTF-8 是 `E6 84 8F`，末字节 0x8F 正好踩在这个坑上，用严格编码器会导致整段 24 个汉字全部还原失败。
  - 实测（临时探针，验完已删，三本真书）：b1 第 1 章标题 `ä½œä¸ºæ„ å¿—å’Œè¡¨è±¡çš„ä¸–ç•Œ_9787100078290` → **`作为意志和表象的世界_9787100078290`**；三本书的章节标题与正文残留 mojibake 命中数均为 **0**；b2 / b3 标题与正文量一字未变（未被误伤），正文抽样确认正常中英文完好。副作用：b1 的 `plainText` 因 30 个乱码字符收敛成 10 个汉字而短了 20 字，阅读位置会有极小漂移。
  - 手动冒烟项见下方「手动冒烟验证」中的乱码三条。

- 2026-09-24：将插件版本号提升到 `0.8.0`（**`Book.plainText` 内容变化，按项目惯例属于 breaking change**：b1 识别出的脚注从 78 条增到 296 条，正文里多出 218 个 `[注N]` 引用点、章末多出 218 条注释条目，阅读位置会有极小漂移）。修复 EPUB 脚注**漏识别**（用户反馈「书里很多脚注点不动」）：
  - **根因**：`collectMutualAnchorFootnotes` 判「成对锚点」时**要求两端锚文本解析出同一个 `[N]` 编号**（`if (footnoteMarkerNumber(peer.label) != number) return`）。但这类转换器给**注释条目**的编号是**按小节重新起算**的：正文引用点是 `[2]`/`[3]`/`[4]`…，同一段章末的注释条目却写 `[1]`/`[2]`/`[1]`…，两端编号天然对不上。全量核对 b1：**296 对**互指锚点里有 **218 对**因编号不等被直接丢掉，只剩 78 对。这个判据从根上就是错的——它把「转换器的编号习惯」当成了结构特征，换个转换器就不成立。
  - **修复 1（改判据）**：把「两端编号必须相等」换成**结构性判据**。文档顺序上先出现的锚点是**引用点**（长在句子中间、常包在 `<sup>` 里），后出现的是**注释条目**；编号改从**引用点**端取（`footnoteMarkerNumber(ref.label)`），不再拿注释条目端的编号去卡。
  - **修复 2（新增 `isLeadingAnchorInBlock`）**：注释条目端改判**锚点是否领衔其所在块**。章末注释条目形如 `<p style="text-indent:2em;"><a id="d1e150" href="#sd1e150">[1]</a> 注释正文…</p>`，锚点是整块第一个内容；正文里的引用点前面是一大段正文（前缀非空白），通不过这一条，从而挡住普通"正文链接 + 返回链接"被误判成脚注。坐标基准与 `findContainingBlock` 命中块一致，用 `MatchGroup.range` 取块内层前缀判断是否全空白。
  - **实测（临时探针，验完已删，三本真书）**：b1 识别到的脚注引用点 **78 → 296**、章末注释条目 **78 → 296**（正好等于互指锚点总对数——结构性判据把 296 对**全部**认出来，一条不多），编号区间按章起算 1..95，含脚注的章从 8 章不变；抽样核对 `ref ↔ 注释条目` 一一对应、正文正常（如 `#[1] → 「原文"P P"是德语化的希腊文…」`）。b2 / b3 **一字未变**（仍是 0 / 0，两本书本来就没有成对锚点），未见新的误报。
  - 手动冒烟项见下方「手动冒烟验证」中的脚注两条。

- 2026-09-24：将插件版本号提升到 `0.8.1`（patch，**非 breaking**：只改**派生**的可点击热区，`Book.plainText` 一字未变）。修复脚注弹窗**显示错注释**（0.8.0 独立回归中暴露的既有缺陷，被 0.8.0 放大）：
  - **根因**：`Book.buildHotSpots` 解析注释正文用的是**全书级** `associateBy { footnoteId }`，但 `footnoteId` 只在**文档内**唯一 —— 不少转换器在不同 XHTML 文档之间复用 `sd1eNN` / `d1eNN` 这类 id（实测 b1 有 15 个 id 出现在 ≥2 个文档）。`associateBy` 撞 key 时**保留最后一个**，于是前面章节的引用点解析到**别的章节**的注释正文。实测 b1 有 **16 / 296** 个引用点点开显示错内容；该缺陷 0.7.0 起就存在 1 处（`sd1e17` / 第一版序），被 0.8.0 新认出的 218 条引用点放大到 16 处。
  - **修复**：把 `associateBy` 换成 `groupBy`（按 id 保留**全部**注释条目，并保持块原有顺序），新增 `resolveFootnoteBody`：引用点取**自己之后第一个同名条目**（`plainStart >= ref.plainEnd`），找不到才退回最后一个同名条目（等价改动前行为，避免把"有正文"变成"空弹窗"）。**这不是启发式而是布局保证**：`appendFootnoteSummary` 把注释汇总区**永远追加在本章末尾**，而引用点必定排在它之前，所以「引用点之后第一个同名条目」必然同章。
  - 实测（临时探针，验完已删，三本真书）：b1 错配 **16 → 0**（含定点复核 `sd1e17` / 第一版序，从错变对）；b1 `plainText` 长度 **488793**、MD5 **`d583a4100c97aa1e74816a52e2113763`**，与 0.8.0 **完全一致**（硬断言）；b2 / b3 的 MD5 仍为 `252b2f00aa2297bbf5e34018e6bc856c` / `cdfa445287f679051921569000a57011`；body 为空的引用点 0 个（没有把"有正文"变成"空弹窗"）。
  - 手动冒烟项见下方「手动冒烟验证」中的脚注同名一条。

- 2026-09-24：**TXT 章节识别增强（0.9.0 第一批；版本号等 EPUB 目录驱动那笔一起升，本笔不动版本号）**。用户反馈「章节识别支持太弱」，TXT 侧病灶在 `ChapterParser.kt`：旧实现只有两条正则（`第N章…` / `chapter N`）且要求整行匹配，`sentencePunctuation` 把**逗号**也当拒绝符，于是「第十二章，温柔的夜」这类合法标题被误杀。本次按「规则分强弱 + 弱规则要布局证据」重构：
  - **规则表（8 条，顺序即优先级）**：① `第N章/回/节/卷/集/部/篇`；② 无「第」的 `卷N/篇N/章N/部N`；③ 英文 `Chapter/Part/Book/Section/Prologue/…`；④ 独立标题词（整行 / 词+`：·、-—(（`+短尾 / 词+空格+短尾）；⑤ Markdown `#` 标题 —— 以上**强规则**，命中即认。⑥ 中文序号行 `一、…`；⑦ 阿拉伯序号行 `1. …`；⑧ 罗马数字行 `IV. …` —— **弱规则**，必须额外满足布局证据。
  - **布局证据**（挡正文里成串的编号列表）：本行是文件首行 / 上一行是空行 / 上一行（非空）以句末标点结尾。于是 `他列出了三点：` 紧跟的 `1. 第一点内容` 通不过（上一行以 `：` 收尾），而 `\n\n一、稻草人手记` 能通过。
  - **三个误判点修正**：阿拉伯数字行分隔符后加负向断言 `(?![0-9０-９])`（挡 `1.5 倍` / `2024.10.01`）；罗马数字行只允许 `、.．` 且**不忽略大小写**（挡 `I am a student`）；独立「开头」章设 200 字下限（前导太短不补，避免几十字的垃圾章节）。另加一条：`第二卷第216页` 这类跨卷页码引用显式拒绝。
  - **`isChapterTitle` 放宽**（本次关键放宽是**逗号移出拒绝符**）：`sentencePunctuation` 从 `[。！？；，,]` 改为 `[。！？；]`；`MAX_TITLE_LENGTH` 40 → 60；`MAX_CHINESE_TITLE_SUFFIX_LENGTH` 24 → 40；叙事连接词字符类补 `之` / `的` / `了`。
  - **`cleanTitle`**：去 Markdown `#` 前缀 → 连续空白（含全角空格 U+3000 / 不换行空格 U+00A0）折叠为单空格 → trim → 去行尾 `。：`。
  - **实现踩到的坑**：`hasLayoutEvidence` 最初"回退时跳过所有连续换行符"，会把上一行的空行一起跳过，导致「空行在前」的弱规则被误判成「上一行是正文」（正向用例只认出 17/20）。改为**只回退一个**行终止符（`\n` / `\r\n` / `\r`）后正确。
  - 实测（构造用例探针，验完已删；没有 TXT 样例书）：正向 **20/20**（含 3 条弱规则）；负向逐条**隔离**测（单行文档里弱规则天然拿到「文件首行」证据，是最坏情况）**0/9 误识别**；长前导（250 字）正确补出「开头」段，短前导（30 字）按设计不补；正向与长前导的区间连续性均为「连续覆盖 `0..len`」。
- 2026-09-24：**前导 clamp（0.9.0 第一批的补漏）**。上一笔遗留的「短前导空洞」按上报方案落地：前导 `< 200` 字 → 首章 `startOffset` **钳到 0**（前导并入首章）；`>= 200` 字 → 补 `Chapter("开头", 0, firstStart)`。二选一，保证 TXT 与 EPUB 的闭包口径一致：**章节区间连续覆盖 `0..len`**。实测（构造用例）：13 字前导 → `第一章[0,30)`；241 字前导 → `开头[0,241)` + `第一章[241,256)`，两段均无空洞。上一笔 dev-record 里「短前导 30 字按设计不补」的描述随之作废。

- 2026-09-24：**脚注配对键改全书唯一（0.9.0 前置重构，推翻 0.8.1 的位置推断）**。0.8.1 的 `resolveFootnoteBody`（取引用点之后第一个同名条目）靠的是「`appendFootnoteSummary` 总把注释追加在本章末尾」这一**布局巧合**；本次注释要改成按 TOC 章节归属（「本章首次引用到的注释写在本章末尾」），同一条注释在后面章节的第二个引用点前面就没有同名 body 了 —— 该前提会被新设计主动破坏，不能再叠第三层位置补丁。改为根治：`EpubFootnote` 加派生属性 `val key get() = "$docId#$id"`（`id` 保持原样，`replaceFootnoteRefs` 还要拿它匹配 `<a id="…">`），引用点哨兵与章末 `FootnoteBodyBlock` 两处产出都改用 `key`；`Book.kt` 的 `groupBy` + `resolveFootnoteBody` 整段删除，回到精确的 `associateBy { it.footnoteId }` —— 配对从「猜位置」回到「查表」，与注释写在第几章完全解耦。实测（临时探针，验完已删）：b1 弹窗错配 **0**（独立 oracle 逐篇重算互指锚点对，296/296 全对），三本 `plainText` MD5 与 0.8.1 基线逐字节一致；顺带确认 b1 有 15 个 id 跨文档复用、涉及 31 条注释，改键后 296 条 key 全不撞车。

- 2026-09-24：**将插件版本号提升到 `0.9.0`（`Book.plainText` 内容变化 + 章节集合变化，按项目惯例属于 breaking change），EPUB 章节改由目录驱动**。用户反馈「章节识别支持太弱」的 EPUB 侧病灶：现状是「一篇 spine 文档 = 一章」，而实测三本样例书里 b1 用 12 篇文档装 71 章、b2 用 15 篇装 92 章 —— **「同文档多章节」是主场景，不是边缘场景**。本次改动：
  - **新增 `book/EpubToc.kt`（纯函数）**：`parseNav` / `parseNcx` / `selectLevel` / `completeTitles`，只吃已解析好的 DOM，XXE 安全配置仍留在 `EpubBookLoader`，可脱离 IDE 单测。**两种目录都必须支持**：b1 / b3 只有 `toc.ncx`、没有 `nav.xhtml`，只认 nav 会直接退化；两者都有时（b2）以 nav 为准。顺带把 `normalizeZipPath` 提到本文件（`internal`）供两侧共用，避免两份实现。
  - **层级选择**：每层条目数落在 `[8,120]` 的层里取**最深**的一层（`[8,120]` 的语义：下界挡「前言/附录」这类零散层，上界挡 b2 的 L3=181 这种"小节"粒度）。选中集合 = 该层条目 **∪ 更浅层里的叶子条目** —— 第二条不能省：「附录」式顶层条目没子项，它本身就是一章，只取选中层会把它整段漏掉。
  - **落点复用既有哨兵机制**：在 `buildChapterBody` **最前面**把 `\u0000TOC:<index>\u0000` 插到目标元素开标签之后（**从后往前插**），`splitMarkedBody` 识别成 `BodyPiece.TocAnchor`，`ChapterWriter` **只记 offset、不写 Block** —— `plainText` 一个字符都不会多。插在最前是为了让 `removeRanges` / `replaceFootnoteRefs` / `replaceImages` 全在同一份字符串上算区间，不需要任何偏移平移补偿。连带加固：`parseAnchor` 与 `isLeadingAnchorInBlock` 判空白/判长度前先剥掉 TOC 哨兵（否则锚文本被哨兵撑大，整对脚注会被 N2 误杀），`isAloneOnLine` 判块级图前同样先剥。
  - **注释按章归属**：`ChapterWriter` 记账「本章首次引用到的注释」（`BodyPiece.FootnoteRef` 顺带带上 `text`，不必反查注释表），`flushFootnotes()` 在记 `tocOffsets` **之前**执行（否则【注释】块会被算进下一章）；从没被任何引用点引用的注释在文档末尾兜底写掉。**无注释的章节一个字符都不多写**（`fresh.isEmpty()` 直接 return，【注释】标题幂等）。
  - **区间闭包**：按 `(spineIndex, offset)` 排序去重（nav/ncx 顺序不保证与 spine 一致），前导 `>= 200` 字补「开头」章、否则首章钳到 0，保证连续覆盖 `0..plainText.length`。
  - **三级兜底**：单条 fragment 定位失败 → 丢弃（正文并入前一段，不留空洞）；全部定位失败 → 「一篇 spine 一章」（顺手把区间拉成连续，消掉两篇之间 `\n\n` 造成的 2 字符空洞）；spine 也空 → `Chapter("全文", …)`。
  - 实测（临时探针，验完已删；三本真书）：章节数 **b1 78 / b2 94 / b3 10**，标题取自目录（b1 的裸「§1」按父标题补全成「第一篇 世界作为表象初论 · §1」）；区间连续覆盖三本**全 true**；b1 `FootnoteBodyBlock` 仍 **296**、【注释】次数 63（≤ 78 章）；b2 的 10 条孤立注释**一条不丢**；弹窗错配 **0**；**降级对照**（抽掉 nav + ncx）12 / 14 / 14 章，三本 `plainText` MD5 **与 0.8.1 基线逐字节一致** —— 证明 TOC 路径下 MD5 的变化只来自「章节切分 + 注释落点」，正文内容本身没动。
  - **已知偏差（已上报）**：b3 是 10 章而不是预估的 13-14。它的 NCX 实测是 **L1=10 / L2=4**（设计文档记的「L1=14」是扁平计数，实际有 4 条嵌在第二层），L2 的 4 条不满足下界 8，选中最深合格层 L1 后并上更浅层叶子仍是 10 条；把下界降到 4 会让 L2 合格，选中集合变成「L2 四条 + L1 六条叶子」，**仍是 10**。要拿到 14 必须把选中集合放宽成 `depth <= L*`，那会顺带把 b1 变成 82、b2 变成 105，与 78/94 冲突 —— 三本不能同时满足，这里按既定规则实现。
  - **另一处实现取舍（已上报）**：spec 里「TOC 模式不调 `extractTitle`」我理解为**章节标题**一律取自目录（确实不调），但**正文里文档标题仍照现状写入**。理由：不写的话 b1/b2/b3 的 `plainText` 会额外少掉十几处标题，超出「MD5 变化只因章节/注释落点」的口径。

## 后续验证步骤

运行：

```powershell
gradle buildPlugin
```

或使用项目自带 Gradle Wrapper：

```powershell
.\gradlew.bat buildPlugin
```

生成的插件 ZIP 位于：

```text
build/distributions/
```

> **真机调参提醒**：`NeatReaderPanel.kt` 里的 `TARGET_CSS_WIDTH_MAX = 1024` 是**猜测值**，站点真实断点必须由真机测量后回填。
> 测量与调参步骤见 `README.md` 的「Neat Reader 缩放调参」一节（拖窗 + 搜 IDE 日志关键字 `Neat Reader 缩放`）。

手动冒烟验证：

- 确认 `Tools -> Novel Reader` 可见。
- 确认工具窗口存在“本地阅读”和“Neat Reader”两个 Tab。
- 确认 `Tools -> Novel Reader -> 打开 Neat Reader` 能切换到内嵌网页阅读器。
- 确认 Neat Reader Tab 能打开 `https://www.neat-reader.cn/webapp`；JCEF 不可用时显示提示，且「重试」和「外部打开」两个兜底按钮可用。
- 确认 Neat Reader Tab 打开书籍后正文可正常阅读；把工具窗口从很窄拖到很宽，全程**都不出现「双列 / 窄正文 + 侧边栏」的宽屏版式**（这是 0.4.7 的首要目标）。
- 确认缓慢并持续拖动工具窗口时，缩放不会逐像素频繁跳变，只会在停止拖动约 150 ms 后整体换一档（验证 resize 防抖与迟滞带）。
- 确认相邻档位之间视觉差异可辨（约 7.6%），不再出现「调了没反应」。
- 确认「缩小」「放大」按钮可用且手动缩放后进入锁定状态；锁定后拖动工具窗口缩放不再自动改变，点「跟随窗口」能解锁并回到自动换档。
- 确认手动缩放后重启 IntelliJ IDEA，缩放档位与锁定状态都能被记住（验证持久化）。
- 确认打开工具窗口、切换到 Neat Reader Tab 时 IDE 无明显卡顿（验证 JCEF 延后创建）。
- 确认在 IDE 日志中能搜到关键字 `Neat Reader 缩放`，日志行包含 `viewportWidth` / `zoomFactor` / `cssWidth` / 档位；同页还能搜到 `Neat Reader 页面回报: metrics innerWidth=...`，用于回填 `TARGET_CSS_WIDTH_MAX`（这是调参的前提，搜不到就无法调参）。
- 确认把工具窗口拖到很宽（1600 px 以上）时，IDE 日志中**不出现** `Neat Reader 缩放已达上限` 的 WARN；出现即说明放大倍率被 `MAX_ZOOM_LEVEL` 夹住、宽屏版式会重现，需按 `README.md`「排查：宽窗口下『保不错版』失效」一节处理（此时改 `TARGET_CSS_WIDTH_MAX` 无效）。
- 确认 Neat Reader 云端下载或打开书籍时不会弹出空白窗口，`window.open` 和 `target="_blank"` 新窗口请求都会留在当前 Tab 内处理（脚本在页面加载完成后注入并带校验重试）。
- 打开 UTF-8 TXT 文件。
- 打开 GBK 或 GB18030 中文 TXT 文件。
- 打开 EPUB 文件，确认能按电子书阅读顺序显示正文。
- 确认 EPUB 章节下拉框、上一章 / 下一章、全书进度和阅读记忆正常。
- 确认 EPUB 脚注引用能在正文显示 `[注N]`，对应注释能在章节末尾汇总。
- 确认脚注引用 `[注N]` **和前后正文排在同一行**，不再被顶到单独一行、前后也不再多出空行（0.6.0 修复项）。
- 确认修脚注换行后**段落分隔没有丢失**：相邻段落之间仍有空行，标题、列表、引用、表格各自成行，不会连成一大坨。
- 确认紧邻图片的空行点下去**不会**弹出图片灯箱；点图片本身仍能弹灯箱（0.6.0 顺带修的命中测试精度问题）。
- 确认脚注引用标记仍带颜色和下划线、鼠标移上去仍是手型、点开弹窗内容正确（脚注三件套不被换行修复破坏）。
- 打开《作为意志和表象的世界》这类**章末带成对脚注**的 EPUB：正文里的 `[N]` 引用点应**绝大多数**都可点、且能弹出对应注释正文（0.8.0 修复项；修前 b1 这类书大量引用点点不动）。
- 确认章末【注释】区把同一章的注释条目都列全了，且弹窗显示的注释正文与书里注释条目一致（0.8.0 修复项）。
- 重点抽查**不同章节里出现同名脚注 id** 的引用点（多见于转换器复用 `sd1eNN` 的老书）：弹窗正文必须与**本章**章末【注释】里那条一致，不能串到别的章节（0.8.1 修复项；修前 16/296 会串章）。
- 打开 EPUB 后看章节下拉框：标题应为**可读中文**，不再出现 `ä½œä¸º…` 这类串（0.7.0 修复项）。
- 换几本不同来源的 EPUB（至少一本中文繁体 / big5 老书）确认标题仍正常，且**没有被修复逻辑误伤**（标题不该莫名变形）。
- 确认 EPUB 标题、列表、引用、表格、图片 alt、ruby 注音和强调文本在纯文本阅读区中仍然可读。
- 确认章节检测和章节导航正常。
- 确认章节边界可以连续阅读，章节尾部和下一章开头可以同屏查看。
- 确认在章节边界附近连续滚动时，不会出现滚轮多次空转或明显停顿。
- 确认章节衔接处连续滚轮不会来回抽搐；如果滚动条已到当前渲染窗口底部或顶部，继续滚动应进入相邻章节窗口。
- 确认从章节上下文预览区继续向下滚动时，不会跳回旧窗口对应章节。
- 确认全书滚动条可以跨章节连续滚动，章节边界不再发生正文整体替换、卡顿、跳回或滚轮空转。
- 确认虚拟阅读组件下拖选文字、复制、本地词典查找、汉典查词和百度搜索仍然可用。
- 确认拖动窗口大小、切换阅读宽度、字号、行距、字体或加粗后，章节和阅读位置不会明显漂移。
- 确认多档字重可以即时生效，重启后仍能恢复；图标按钮模式和文字按钮模式下显示符合预期。
- 确认持续拖动窗口大小时阅读区不会频繁整本重排，松开或停止缩放后能回到原可视锚点附近。
- 确认行尾最后一个字、跨行文本和反向拖选都能正常选中并用于右键查词。
- 确认拖动工具窗口大小、修改字号、行距或阅读宽度后，当前位置不会明显漂移。
- 确认正文每行最后一个字可以正常拖选和右键查词。
- 确认字号、行距、字体选择正常。
- 确认文字颜色、阅读主题、阅读宽度、进度显示、快捷键、隐藏光标开关和按钮显示方式切换正常。
- 确认阅读区选中文本后右键菜单可用，本地词典查找、汉典查词、百度搜索和复制正常。
- 重启 IntelliJ IDEA 后确认阅读状态可以恢复；调整窗口大小、字号、行距或阅读宽度后，恢复位置仍应接近上次阅读文字。
- 打开带图的 EPUB（如 z-library 版《作为意志和表象的世界》），确认正文里的图片以图片块形式出现，且**不会把正文挤成一行半行**（图片高度只取文件头探测的宽高）。
- 确认图片出现时阅读区**不发生跳动、阅读位置不漂移**；图片解码完成后只重绘，滚动条总高度不变（这是 §8 红线，若滚动条高度变化说明解码回调触发了重排）。
- 确认 SVG 或拿不到内建尺寸的图片渲染为固定占位框，不会出现高度塌陷或撑满整屏。
- 点击正文里的脚注引用（如 `[注1]`），确认弹出注释弹窗且正文为完整注释；弹窗标题保留书里原本的标记（如 `[1]`）。
- 确认点脚注时**不会**顺带把 `[注1]` 选中（热区优先于选区），划词右键菜单语义不变。
- 确认鼠标移到脚注引用和图片上时变成手型光标；**勾选「隐藏光标」后手型光标仍然出现**，注解和图片仍然点得到。
- 点击正文图片确认灯箱打开，滚轮可缩放、可拖拽平移、「1:1」「适配」按钮可用，Esc 能关闭。
- 确认灯箱首屏先显示已有小图、随后替换为高清图，期间阅读区滚动条总高度不变、阅读位置不漂移。
- **确认弹窗 / 灯箱弹出与关闭不会引起阅读区重排**：记录打开弹层前的阅读位置，关闭后应停在同一处；并在 IDE 日志中确认没有出现因弹层触发的锚点回写（防 0.3.2「阅读记忆被覆盖」回归）。
- 确认弹层关闭后焦点回到阅读区，方向键 / 空格仍然能滚动正文。
- 确认打开 0.4.7 之前保存的阅读记录时，位置恢复不因 `Book` 结构化改造而漂移（plainText 拼接口径必须逐字一致）。
- 确认把工具窗口从很窄拖到很宽后，正文里的图片**会变清晰**（0.5.1 前会一直显示窄宽度时解码的糊图、永不重解）。
- 点击图片打开灯箱，确认图片清晰、不变形；IDE 日志搜 `图片灯箱解码完成`，位图实际宽高应等于或接近原图（修复前会是小图尺寸）。
- 确认正文里的 `[注N]` 显示颜色与正文不同且带下划线；章末注释条目的 `[注N]` 前缀同色，但注释正文仍是正文颜色（不整条染蓝）。
- 依次切换护眼 / 纸张 / 暗色三个主题，确认 `[注N]` 在三种背景上都清晰可辨；切到「跟随 IDE」时也应能看清。
- 确认标记跨行时（折行把 `[注N]` 切在行尾 / 行首）、以及一行内有多个相邻标记（如 `[注6][注7]`）时着色正确，没有多染、漏染或错位。
- 确认 `[注N]` 着色后划词选中、复制、右键查词不受影响；选中状态下 `[注N]` 仍然看得出来。
- 确认翻到不含脚注的大段正文时，滚动与选中手感和此前一致（验证「只在含脚注的行才走分段绘制」，无性能退化）。
